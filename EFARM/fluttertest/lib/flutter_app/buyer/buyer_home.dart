
import 'package:flutter/material.dart';

class BuyerHome extends StatelessWidget{
@override
Widget build(BuildContext context){
return Scaffold(
appBar:AppBar(title:Text("Buyer Home")),
body:Center(child:Text("Product List / Order Button")),
);
}
}
